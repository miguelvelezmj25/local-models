{
  "region" : "f813ca77-a78b-463b-a225-bcbf066d3c59",
  "model" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.8166E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.848E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.53695E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.8818E7,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.8054E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.80085E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.8538E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 4.2965E7
  },
  "modelToMin" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.2056E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.249E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.2109E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.3186E7,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.7863E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.3098E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.4349E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.8947E7
  },
  "modelToMax" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 3.8965E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.8848E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 3.8105E7,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 5.3697E7,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 4.3638E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 4.8233E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 4.297E7,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 5.4351E7
  },
  "modelToDiff" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 6909000.0,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 6358000.0,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 5996000.0,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 2.0511E7,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 5775000.0,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 1.5135E7,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 8621000.0,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 1.5404E7
  },
  "modelToSampleVariance" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 1.4284130333333336E13,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 1.2739948E13,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 9.843779583333334E12,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 1.1230086433333334E14,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 1.0761360333333334E13,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 4.0537854E13,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 1.8585331E13,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 6.3844756E13
  },
  "modelToConfidenceInterval" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ 2.7007029189438358E7, 4.578430414389497E7 ],
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ 2.7739351428695302E7, 4.5472648571304694E7 ],
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ 3.024581947966615E7, 4.023068052033385E7 ],
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ 1.557540380010587E7, 6.82252628665608E7 ],
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ 3.1702574031694975E7, 4.800075930163835E7 ],
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ 2.9205786529476818E7, 4.946821347052318E7 ],
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ 2.7909706575343415E7, 4.9328293424656585E7 ],
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ 2.557201590773709E7, 6.526998409226291E7 ]
  },
  "modelToCoefficientOfVariation" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 0.11249658254194919,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 0.10563155147043524,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 0.0946009606716521,
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 0.2739909244762544,
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 0.08917624965647308,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : 0.1719721338403519,
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 0.12093339144948628,
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : 0.19057592241769386
  },
  "modelToPerfHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0381660",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0384800",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0353695",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0388180",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0380540",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0380085",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0385380",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0429650"
  },
  "modelToMinHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0320560",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0324900",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0321090",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0331860",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0378630",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0330980",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0343490",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0389470"
  },
  "modelToMaxHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0389650",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0388480",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0381050",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0536970",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0436380",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0482330",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0429700",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0543510"
  },
  "modelToDiffHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0069090",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0063580",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0059960",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0205110",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0057750",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0151350",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0086210",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0154040"
  },
  "modelToSampleVarianceHumanReadble" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0000143",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0000127",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0000098",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0001123",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0000108",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0000405",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0000186",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.0000638"
  },
  "modelToConfidenceIntervalHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ "0.0270070", "0.0457843" ],
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ "0.0277394", "0.0454726" ],
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ "0.0302458", "0.0402307" ],
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ "0.0155754", "0.0682253" ],
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ "0.0317026", "0.0480008" ],
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : [ "0.0292058", "0.0494682" ],
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ "0.0279097", "0.0493283" ],
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : [ "0.0255720", "0.0652700" ]
  },
  "modelToCoefficientOfVariationHumanReadable" : {
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.1124966",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.1056316",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0946010",
    "(!MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.2739909",
    "(MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.0891762",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && !RAM_BUFFER_SIZE_MB)" : "0.1719721",
    "(MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.1209334",
    "(!MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS && RAM_BUFFER_SIZE_MB)" : "0.1905759"
  }
}