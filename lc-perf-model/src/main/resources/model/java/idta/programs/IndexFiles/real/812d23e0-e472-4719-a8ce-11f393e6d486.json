{
  "region" : "812d23e0-e472-4719-a8ce-11f393e6d486",
  "model" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToMin" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToMax" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToDiff" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToSampleVariance" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToConfidenceInterval" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ]
  },
  "modelToCoefficientOfVariation" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0,
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : 0.0
  },
  "modelToPerfHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  },
  "modelToMinHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  },
  "modelToMaxHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  },
  "modelToDiffHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  },
  "modelToSampleVarianceHumanReadble" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  },
  "modelToConfidenceIntervalHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ],
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : [ ]
  },
  "modelToCoefficientOfVariationHumanReadable" : {
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!RAM_BUFFER_SIZE_MB && !CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000",
    "(RAM_BUFFER_SIZE_MB && CHECK_PENDING_FLUSH_UPDATE && !MAX_TOKEN_LENGTH && MAX_BUFFERED_DOCS)" : "0.0000000"
  }
}