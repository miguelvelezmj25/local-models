{
  "region" : "01d66b26-590b-4dc6-b7a8-3c457ec17d26",
  "model" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 7872500.0,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 1.0489E7,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 7918000.0,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.04815E7,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 3.15985E7,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.3696E7,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.0521E7,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 7860500.0
  },
  "modelToMin" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.1038E7,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 1.5766E7,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0
  },
  "modelToMax" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 2.0871E7,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.0898E7,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.1122E7,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 3.718E7,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 5.2433E7,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 3.6574E7,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 2.6413E7,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.573E7
  },
  "modelToDiff" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 2.0871E7,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.0898E7,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.1122E7,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 3.718E7,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 3.1395E7,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 2.0808E7,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 2.6413E7,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.573E7
  },
  "modelToSampleVariance" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 6.101741798214286E13,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 7.651358955357142E13,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 5.53844885625E13,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.5656711098214284E14,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 6.38590166E13,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 6.044426741071429E13,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.6443550307142856E14,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 4.6517356E13
  },
  "modelToConfidenceInterval" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ 2673910.710769655, 1.5734839289230345E7 ],
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ 1879278.8894001963, 1.6504971110599805E7 ],
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ 3303585.3602054743, 1.1234789639794525E7 ],
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ 775014.3980169334, 2.1696735601983067E7 ],
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ 2.76590487944448E7, 3.61754512055552E7 ],
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ 1.8456904148953184E7, 3.1456345851046816E7 ],
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ 0.0, 0.0 ],
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ 1129752.4584142286, 2.2570747541585773E7 ],
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ 1511533.5601360379, 1.2915466439863961E7 ]
  },
  "modelToCoefficientOfVariation" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.1484374112226203,
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.25428399766657545,
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : 0.3212592694013105,
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : 0.0,
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 1.115923040690395,
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : 0.0
  },
  "modelToPerfHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0078725",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0104890",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0079180",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0104815",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0315985",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0236960",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0105210",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0078605"
  },
  "modelToMinHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0210380",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0157660",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000"
  },
  "modelToMaxHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0208710",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0208980",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0211220",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0371800",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0524330",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0365740",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0264130",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0157300"
  },
  "modelToDiffHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0208710",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0208980",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0211220",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0371800",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0313950",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0208080",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0264130",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0157300"
  },
  "modelToSampleVarianceHumanReadble" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000610",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000765",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000554",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0001566",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000639",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000604",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0001644",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000465"
  },
  "modelToConfidenceIntervalHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ "0.0026739", "0.0157348" ],
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ "0.0018793", "0.0165050" ],
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ "0.0033036", "0.0112348" ],
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ "0.0007750", "0.0216967" ],
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ "0.0276590", "0.0361755" ],
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : [ "0.0184569", "0.0314563" ],
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : [ "0.0000000", "0.0000000" ],
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ "0.0011298", "0.0225707" ],
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : [ "0.0015115", "0.0129155" ]
  },
  "modelToCoefficientOfVariationHumanReadable" : {
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "1.1484374",
    "(!DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.2542840",
    "(DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && TEMPORARY)" : "0.3212593",
    "(!DUPLICATES && !SEQUENTIAL && !MAX_MEMORY && !TEMPORARY)" : "0.0000000",
    "(DUPLICATES && !SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "1.1159230",
    "(!DUPLICATES && SEQUENTIAL && MAX_MEMORY && TEMPORARY)" : "0.0000000"
  }
}