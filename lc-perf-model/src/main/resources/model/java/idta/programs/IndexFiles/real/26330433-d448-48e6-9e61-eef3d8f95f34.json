{
  "region" : "26330433-d448-48e6-9e61-eef3d8f95f34",
  "model" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToMin" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToMax" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToDiff" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToSampleVariance" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToConfidenceInterval" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : [ 0.0, 0.0 ],
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : [ 0.0, 0.0 ]
  },
  "modelToCoefficientOfVariation" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : 0.0,
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : 0.0
  },
  "modelToPerfHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  },
  "modelToMinHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  },
  "modelToMaxHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  },
  "modelToDiffHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  },
  "modelToSampleVarianceHumanReadble" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  },
  "modelToConfidenceIntervalHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : [ "0.0000000", "0.0000000" ],
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : [ "0.0000000", "0.0000000" ]
  },
  "modelToCoefficientOfVariationHumanReadable" : {
    "(MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(!MAX_TOKEN_LENGTH && !RAM_BUFFER_SIZE_MB && !MAX_BUFFERED_DOCS)" : "0.0000000",
    "(MAX_BUFFERED_DOCS || RAM_BUFFER_SIZE_MB)" : "0.0000000"
  }
}